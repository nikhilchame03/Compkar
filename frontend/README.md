# COMPKAR Frontend

React + Vite + Tailwind + shadcn-style component setup.

## Run

```bash
npm install
npm run dev
```

Frontend runs at: http://127.0.0.1:5173

Backend (Django) runs separately inside `../backend`.
